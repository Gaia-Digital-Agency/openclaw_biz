# TOOLS.md

## Available MCP Servers

- `filesystem`
- `fetch`
- `playwright`
- `gmail` — Email operations (azlan@gaiada.com)
- `google-drive` — Google Drive file management

## Delegation

- Delegate substantive analysis to Case (`casey`).
- Delegate email, drive, and document tasks to Case (`casey`).
- Use `sessions_spawn` for delegation (never `sessions_send`).
